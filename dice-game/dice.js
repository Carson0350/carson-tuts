/*

initilize game 
    set dice value to default
    set input value = 0
    set max num = 0 
    set array = []

imput a number that the user types between 1 - 10 (x)

    multiply (x) by 6 to get max possible score

    make an array to push roll values too

roll the dice the (x) number of times

    generate num between 1 & 6 (x) times

    push each number to roll values array

    add the roll values together 

    if score add up to be greater than max number / (x) user wins

    if else they lose

run initilize function

*/
